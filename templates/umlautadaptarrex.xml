<?xml version="1.0"?>
<Container version="2">
  <Name>UmlautAdaptarrEX</Name>
  <Repository>lexfi/umlautadaptarrex:latest</Repository>
  <Registry>https://hub.docker.com/r/lexfi/umlautadaptarrex</Registry>
  <Branch>
    <Tag>latest</Tag>
    <TagDescription>Latest stable release</TagDescription>
  </Branch>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>

  <Support>https://github.com/xpsony/UmlautAdaptarrEX/issues</Support>
  <Project>https://github.com/xpsony/UmlautAdaptarrEX</Project>
  <ReadMe>https://github.com/xpsony/UmlautAdaptarrEX/blob/main/README.en.md</ReadMe>
  <Discord>https://discord.gg/src6zcH4rr</Discord>

  <Category>Tools: MediaApp:Other</Category>
  <WebUI>http://[IP]:[PORT:5007]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/xpsony/UmlautAdaptarrEX-Unraid-Template/main/templates/umlautadaptarrex.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/xpsony/UmlautAdaptarrEX-Unraid-Template/main/icon.svg</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires/>

  <MinVer>6.12</MinVer>
  <License>MIT</License>

  <Changes>
###1.2.5 - 2026-07-10
A maintenance release: all dependencies and the build toolchain refreshed, CI and the dev container moved to Node 26 (the production image already ran Node 26), and the published Docker `:latest` image is now automatically rebuilt every 2 days to pick up OS / base-image security patches between releases. No schema changes, no configuration changes.

**Improvements**
- Automatic Docker security rebuilds: a scheduled workflow now rebuilds the latest stable release image every 2 days with a fresh base image and OS packages, so `:latest` picks up Debian/Node security patches without waiting for a new release. It also publishes a `:&lt;version&gt;-&lt;sha&gt;` variant; the immutable `:&lt;version&gt;` tag is left untouched.

**Security &amp; Maintenance**
- Dependency refresh: all packages bumped to their latest patch/minor -- pnpm 11.3.0 -&gt; 11.11.0, Fastify 5.8.5 -&gt; 5.10.0, Next.js 16.2.9 -&gt; 16.2.10, recharts 3.8.1 -&gt; 3.9.2, lucide-react 1.21.0 -&gt; 1.24.0, undici 8.5.0 -&gt; 8.7.0, the Radix UI set, plus dev tooling (ESLint 10.6.0, Vitest 4.1.10, Vite 8.1.4, Prettier 3.9.4, tsx 4.23.0, typescript-eslint 8.63.0, Playwright 1.61.1). `pnpm audit --prod` reports no known vulnerabilities in the shipped runtime dependencies.
- Node 26 across the board: CI and the dev container now run on Node 26, matching the production image; the server bundle targets Node 24. GitHub Actions bumped (`actions/checkout` v7, `actions/cache` v6). Dependabot dependency PRs now target the `dev` branch and auto-merge after CI.

**Upgrade notes**
- No action needed -- this release has no schema changes and no configuration changes.

###1.2.4 - 2026-06-21
A stability and hardening release: title-provider syncs and the supervisor no longer hang on stalled connections, the indexer proxy and the admin/setup endpoints are hardened, and several title-matching and Web UI bugs are fixed. No schema changes.

**Fixes**
- Operation-mode descriptions now show the actually-configured ports (#30): the mode texts in the setup wizard and Settings -&gt; Operation mode no longer hard-code 5005/5006 but use the resolved ports (`UMLAUTADAPTARREX_*_PORT` override &gt; stored/default). Thanks to xopez (https://github.com/xopez) for reporting.
- Sync no longer hangs on a stalled provider: TVDB, pcjones and TMDB requests now carry request timeouts, so a single unresponsive title provider can no longer block the whole sync indefinitely.
- One bad provider no longer aborts the chain: a provider that errors (e.g. pcjones on a network failure) is now logged and skipped so the remaining providers still contribute, instead of discarding already-merged results.
- Title matching: titles containing tabs or line breaks are no longer collapsed into a single word, and leading articles (`Der`/`Die`/`Das`/`The`/...) are now stripped regardless of capitalization, so lowercase titles produce the same search variations. Readarr external-ID titles now strip the active language's articles, not just English "the".
- Boot &amp; restart robustness: a failed database-migration launch now surfaces an error instead of hanging the boot forever, and a Web UI process that ignores `SIGTERM` is now force-stopped within the grace window so it can no longer orphan the Web UI port on restart. The admin Restart now waits for its response to be sent before tearing down.

**Improvements**
- Indexer proxy hardening: the plain-HTTP relay path now only allows ports 80/443 (matching the HTTPS-CONNECT path), cleans up its sockets and adds a 120s idle timeout -- closing an SSRF / open-relay gap and a socket leak.
- Reduced database load on large installs: the session last-used timestamp is now written at most once every 5 minutes instead of on every request, and "Recheck missing titles" scans the cache in bounded batches instead of loading the whole table into memory at once. Request-history rows now cap the stored domain/query length.

**Security &amp; Maintenance**
- Auth-surface hardening: the unauthenticated `setup-status` and `plugins` endpoints are now rate-limited, and `setup-status` no longer discloses the Prowlarr host or proxy username once setup is complete (the Prowlarr API key was never exposed). The Prowlarr admin actions (preview/import/test/save) are now rate-limited too.
- Added a Content-Security-Policy header; generated passwords now use cryptographically-secure randomness only (no weak fallback, no character bias). Secret-mask detection was tightened so a real stored secret is never mistaken for the mask, while Prowlarr's asterisk masking is still recognized.

**Web UI fixes**
- Fixed a race when closing the Prowlarr-import dialog while it was still loading, a double-submit window on import, and live-log rows shifting/flickering as new lines arrive. The dashboard and instances pages now show a clear error with a retry button when a request fails, instead of looking empty.

**Upgrade notes**
- No action needed -- this release has no schema changes and no configuration changes.

###1.2.3 - 2026-06-06
Lets the container run fully unprivileged and ships as a TrueNAS Community app. Until now the image required root at startup; root is now used only for the one-time `/data` ownership fix and the application process never runs as root. No schema changes.

**Features**
- TrueNAS Community app: UmlautAdaptarrEX is now available in the TrueNAS app catalog (https://apps.truenas.com/catalog/umlautadaptarrex_community/) -- search for "UmlautAdaptarrEX" under Apps -&gt; Discover Apps to install. The app is maintained by xopez (https://github.com/xopez), many thanks.

**Improvements**
- Unprivileged container startup: The image can now be started directly as a non-root user (`docker run --user`, a compose `user:` entry, Kubernetes `runAsUser`, or the TrueNAS app's `run_as` field). In that case the entrypoint skips the `chown`/`gosu` step and runs directly under the given UID/GID, so the container no longer needs the `CHOWN`/`SETUID`/`SETGID` capabilities or root, provided the `/data` volume is already owned by that UID/GID.

**Security &amp; Maintenance**
- root only for ownership setup: When the container does start as root (the default), root is used **only** for the one-time `chown` of `/data`; the entrypoint then drops to `PUID:PGID` via `gosu` before launching the app, so the application process (`node start.mjs`) never runs as root. The startup path is fully backward-compatible with the existing root-by-default compose setup.

**Upgrade notes**
- No action needed for the default Docker / compose setup. To run unprivileged, pre-own the `/data` volume with your target UID/GID and start the container with that user (e.g. `--user 1000:1000`); `PUID`/`PGID` are ignored in that mode since the orchestrator already sets the UID/GID.

###1.2.2 - 2026-06-05
Adds a Proxmox LXC community-script, surfaces the actual service ports in the UI, and fixes API proxying plus the setup flow behind Docker NAT. Includes one additive database migration (`AdminUser.lastSeenChangelog`), applied automatically on start.

**Features**
- Proxmox LXC community-script installer: A one-line command creates an LXC container on Proxmox VE, installs UmlautAdaptarrEX and prompts for the service ports during setup. The installer is self-hosted from this fork. Still in development and not yet fully tested, see the README before using it.
- Service ports in the UI: Settings -&gt; Advanced now shows the active Fastify API port and Web UI port read-only alongside the editable proxy port, so the bound ports are visible without inspecting the environment.

**Improvements**
- Changelog shown once per user: The what's-new dialog tracks its seen-state per admin account in the database (`AdminUser.lastSeenChangelog`) instead of per browser. Each user sees it exactly once and, after confirming, it stays dismissed across browsers and devices until a newer release ships.
- Branded port variables only: Service ports are now read only from `UMLAUTADAPTARREX_LEGACYAPI_PORT` / `UMLAUTADAPTARREX_WEBUI_PORT` / `UMLAUTADAPTARREX_PROXY_PORT`. The legacy `PORT` and `WEB_PORT` fallbacks (still accepted in 1.2.1) have been removed; the compose files and `.env.example` already use the branded names.

**Fixes**
- Runtime API proxying: The Web UI now reverse-proxies `/api/*` at runtime instead of baking the API port into the standalone build. A custom `UMLAUTADAPTARREX_LEGACYAPI_PORT` no longer left `/api/health` and the *Arr icons failing with `ECONNREFUSED`, and static `/arr/*.svg` icons are no longer redirected to `/setup` during the wizard.
- Setup behind Docker NAT: The pre-setup instance test (`/api/auth/instances/test`) no longer hard-blocks private/LAN targets by default; it follows the SSRF-strict toggle (`Setting.blockPrivateInstanceHosts`, default off). Connecting to Sonarr/Radarr on the same LAN now succeeds out of the box during the wizard, while strict mode restores loopback-only behaviour for cloud / multi-tenant operators.

**Security &amp; Maintenance**
- Auth-surface hardening: `/api/auth/me` is now rate-limited per IP (60 / min).
- Dependencies updated: All pnpm packages bumped to their latest patch/minor releases -- Next.js `16.2.7`, React / React-DOM `19.2.7`, `@tanstack/react-query` `5.101.0`, plus dev tooling (`eslint-config-next`, `typescript-eslint`, `concurrently`, `@types/react`). No behaviour changes; `pnpm audit` reports no known vulnerabilities.

**Upgrade notes**
- The new `AdminUser.lastSeenChangelog` column is applied automatically by `prisma migrate deploy` on start. If you relied on the `PORT` or `WEB_PORT` environment variables, switch to `UMLAUTADAPTARREX_LEGACYAPI_PORT` / `UMLAUTADAPTARREX_WEBUI_PORT`.
- Ports must stay 1:1: each branded port variable sets BOTH the in-container bind port and the published host port. If you remap a port, set the host port, the container port and the matching `UMLAUTADAPTARREX_*_PORT` variable to the SAME value.

###1.2.0
Adds the Prowlarr indexer-patch dialog, plus a dependency refresh and a Docker healthcheck fix. No schema changes.

**Features**
- Prowlarr indexer patching: A new dialog lists your Prowlarr indexers and lets you select which ones to patch for UmlautAdaptarrEX. Patching tags the indexer with `umlautadaptarrex` and switches its Prowlarr base URL from `https://` to `http://` so requests flow through the local proxy and titles get rewritten; de-selecting reverts both. Available in the setup wizard and any time under Settings -&gt; Prowlarr. The dialog explains why the switch is needed and that the connection to the indexer itself stays HTTPS (no unencrypted traffic leaves your system).

**Fixes**
- Bare-metal start without Docker: `pnpm prod` (and `pnpm build:prod &amp;&amp; pnpm start:prod`) now boots correctly on a host without Docker. The supervisor finds the Next.js standalone server on bare-metal installs and assembles its `.next/static` and `public` assets next to it on boot, so no manual copy step is needed.

**Build &amp; Tooling**
- Docker healthcheck: Switched the container healthcheck from `wget --spider` to `wget -qO- … &gt;/dev/null` back.

**Dependency Updates**
- concurrently 9.2.1 -&gt; 10.0.1 (major; affects only the dev script)
- next-intl 4.12.0 -&gt; 4.13.0
- lucide-react 1.16.0 -&gt; 1.17.0
- react-hook-form 7.76.1 -&gt; 7.77.0
- lru-cache 11.5.0 -&gt; 11.5.1
- eslint 10.4.0 -&gt; 10.4.1, typescript-eslint 8.59.4 -&gt; 8.60.0
- vite 8.0.14 -&gt; 8.0.16, vitest / @vitest/coverage-v8 4.1.7 -&gt; 4.1.8
- tsx 4.22.3 -&gt; 4.22.4

###1.1.1 - 2026-05-25
**Fixes**
- Lidarr/Readarr sync no longer crashes on duplicate album/book titles: Library rows are now keyed by `normalize("{artist} {album}")` for Lidarr and `normalize("{book} {author}")` for Readarr instead of just the album/book title. Previously libraries with multiple albums or books sharing a title across artists/authors ("Greatest Hits", "Live", "Best Of", "Self-Titled", …) collided on the SearchItem unique constraint and aborted the sync with Prisma P2002. As a side effect the legacy `/api?t=search&amp;cat=3000…` getByExternalId lookup now actually resolves, because Prowlarr's `?q=Artist Album` query normalizes to the same key the row was stored under.
- Sync dedup defense-in-depth: `persistAndReindex` now deduplicates items by externalId before writing, so a stray duplicate in the upstream payload no longer aborts a 50-item chunk transaction. Duplicates are counted and warned in the log; SyncRun.itemsCount reflects the deduped count.
- Scheduler provider gate: `POST /api/admin/sync` and the scheduled tick no longer return no_provider for Lidarr/Readarr-only setups. The gate now only blocks when at least one Sonarr/Radarr instance is enabled, because only those two consult a title provider during sync.

**Upgrade notes**
- No manual resync required. On the next sync (automatic or via "Sync now" in the dashboard) the stale rows with old externalIds are removed.

###1.1.0 - 2026-05-24
**Security &amp; Auth Hardening**
- Session-ID is rotated on successful admin login (session-fixation fix).
- CSRF double-submit check is properly awaited in admin middleware; CSRF secret length validated up-front.
- Login enumeration hardened: unknown users now run a dummy Argon2 verify so response time matches the wrong-password path.
- Session and CSRF cookies forced to `Secure` over HTTPS regardless of `NODE_ENV`.
- Shared log redactor masks `apiKey`, `password`, `prowlarrApiKey` etc. (including legacy routes and WebSocket broadcasts).
- Third-party secrets (TMDB / TVDB / Prowlarr / proxy password) masked in admin settings responses.
- `/api/health` no longer leaks `uptime`; WebSocket upgrade errors return 400 instead of crashing.

**Setup Wizard**
- Concurrent setup completions detected (Prisma P2002) and re-checked instead of failing.
- Unknown plugin IDs rejected up-front.
- Outbound SSRF probes (Prowlarr connect) gated until setup is authenticated.

**Sync Performance**
- `runSync` now fans out across all enabled Sonarr / Radarr / Lidarr / Readarr instances in parallel (wall-time = max-of-instances instead of sum).
- Lidarr / Readarr nested fetches batched at concurrency 8.
- TMDB bulk lookups: parallel batches of 10, capped at ~20 req/s -- a 500-item bulk drops from ~2 min to ~25 s.
- TVDB bulk lookups: parallel batches of 5, capped at ~10 req/s.
- Rewritten concurrency-safe `HostRateLimiter` (per-host promise chain) to fix a race that leaked the rate budget under parallelism.
- `persistAndReindex` splits the mega-transaction into chunks of 50 so the SQLite writer lock is released between chunks.

**Providers**
- TMDB bulk lookups switched to `Promise.allSettled` -- a single failing ID no longer aborts the batch.
- TVDB token-refresh retry guard prevents 401 storms from looping forever.
- Rate limiter clamps negative `Retry-After` values; scheduler watchdog unblocks stuck sync runs.

**Build &amp; Tooling**
- pnpm bumped to 11.3.0 across Dockerfile, devcontainer and `package.json#packageManager`.
- ESLint 9.39.4 -&gt; 10.4.0; `@types/node` 24 -&gt; 25; React version pinned to 19 in ESLint config.

Full changelog: https://github.com/xpsony/UmlautAdaptarrEX/blob/main/CHANGELOG.md
  </Changes>

  <Overview>UmlautAdaptarrEX - Umlaut and multi-language title proxy for Sonarr, Radarr, Lidarr, Readarr and Prowlarr. Complete Next.js + Fastify + Prisma rewrite of the legacy .NET UmlautAdaptarr.&#xD;
&#xD;
Sits between your *Arr stack and the indexers, rewrites searches and Newznab/Torznab responses on the fly so releases with umlauts, German titles, TVDB aliases or bad naming are reliably found, downloaded and imported. Ships a full Web UI with setup wizard, dashboard, sync runs, request and rename history, and live WebSocket logs. Configuration and title cache are stored in a persistent SQLite DB under /data - no external database required.&#xD;
&#xD;
Highlights: Sonarr / Radarr (native via alternateTitles + optional TMDB) / Lidarr / Readarr / Prowlarr / NZB Hydra support, multiple instances per *Arr type, language plugins (German umlauts, Swedish umlauts, French accents) that can be combined, configurable title providers (TVDB, TMDB, db-cache), optional release renaming, two operating modes (Prowlarr indexer proxy on 5006 or direct as indexer on 5005), German + English UI.</Overview>

  <Description>UmlautAdaptarrEX - Umlaut and multi-language title proxy for Sonarr, Radarr, Lidarr, Readarr and Prowlarr. Full Next.js + Fastify + Prisma + SQLite rewrite of the legacy .NET UmlautAdaptarr.&#xD;
&#xD;
[b]What it solves[/b]&#xD;
- Releases with umlauts are often not matched correctly by the *Arrs (search uses `o` instead of `oe`/`ö`, indexer mapping is missing).&#xD;
- Sonarr / Radarr expect the English title from TheTVDB / TMDB. For German productions this leads to errors like "Found matching series/movie via grab history, but release was matched to series by ID".&#xD;
- Bad release naming (e.g. missing GERMAN tag) is optionally corrected so the *Arrs detect releases properly.&#xD;
&#xD;
[b]Features[/b]&#xD;
- Sonarr, Radarr (native, via alternateTitles + optional TMDB), Lidarr, Readarr support&#xD;
- Prowlarr and NZB Hydra integration; Newznab (Usenet) and Torznab (Torrent) supported&#xD;
- Multiple instances per *Arr type (e.g. 2x Sonarr)&#xD;
- Detection of releases with German title and TVDB aliases&#xD;
- Optional renaming of badly named releases&#xD;
- Language plugins (combinable per library): German umlauts (default), Swedish umlauts, French accents&#xD;
- Multiple title providers with configurable order: TVDB, TMDB, internal db-cache&#xD;
- Web UI with setup wizard, login, dashboard, instance manager, sync runs and full request / rename history&#xD;
- Live logs via WebSocket directly in the browser&#xD;
- Persistent SQLite database (Prisma) - no cache loss after restart&#xD;
- Internationalized UI: German and English&#xD;
&#xD;
[b]Operating modes[/b]&#xD;
1. Prowlarr indexer proxy (recommended, port 5006): UmlautAdaptarrEX is registered as an HTTP indexer proxy with HTTP-CONNECT tunneling, no per-indexer URL changes in the *Arrs.&#xD;
2. Direct as indexer (port 5005): enter `http://[IP]:5005/&lt;apiKey&gt;/&lt;indexer-host&gt;` per indexer in Sonarr / Radarr / Lidarr / Readarr.&#xD;
&#xD;
[b]Ports[/b]&#xD;
- 5005: Fastify API + WebSocket logs - URL you point your *Arrs at, also serves /api/admin, /api/auth, /titlelookup and /ws/logs.&#xD;
- 5006: TCP HTTP-CONNECT proxy for Prowlarr indexers - configure as Prowlarr indexer proxy with Basic auth (default user `UmlautAdaptarr`, password auto-generated and shown in the setup wizard).&#xD;
- 5007: Next.js Web UI - on first start, open `http://[IP]:5007/setup` and the wizard walks you through account, mode, plugins, *Arrs, Prowlarr and proxy credentials.&#xD;
&#xD;
[b]Port remapping (1:1 mapping required)[/b]&#xD;
Each branded port variable (UMLAUTADAPTARREX_LEGACYAPI_PORT / UMLAUTADAPTARREX_WEBUI_PORT / UMLAUTADAPTARREX_PROXY_PORT) sets BOTH the in-container bind port and the published host port, so the host side and container side must always be identical. To run on a non-default port, change the host port, the container port AND the matching UMLAUTADAPTARREX_*_PORT variable to the SAME value. A mismatched (non-1:1) mapping breaks /api proxying and the indexer URLs your *Arrs point at.&#xD;
&#xD;
[b]Storage[/b]&#xD;
Everything (settings, instances, API keys, title cache, sync runs, request and rename history) lives in a single SQLite file under /data. Map /data to your appdata share (default `/mnt/user/appdata/umlautadaptarrex`) and back it up like any other *Arr appdata.&#xD;
&#xD;
[b]Permissions[/b]&#xD;
The container chowns /data to PUID:PGID on every start (default 99:100, the Unraid `nobody:users` pair), so no manual `chown` is required. Adjust PUID / PGID only if your appdata share uses a different owner.</Description>

  <Config Name="Web UI Port" Target="5007" Default="5007" Mode="tcp"
          Description="HTTP port for the Next.js web UI. Open this in your browser to reach the setup wizard and admin panel. IMPORTANT: the host port and the container port MUST be identical (1:1 mapping). To use a non-default port, change this value AND set the matching UMLAUTADAPTARREX_WEBUI_PORT variable below to the SAME number."
          Type="Port" Display="always" Required="true" Mask="false">5007</Config>

  <Config Name="API Port" Target="5005" Default="5005" Mode="tcp"
          Description="Fastify API + WebSocket port. This is the URL you point your *Arr instances at (e.g. http://unraid-ip:5005/&lt;apikey&gt;/sonarr/api). Also serves /api/admin, /api/auth, /titlelookup and /ws/logs. IMPORTANT: the host port and the container port MUST be identical (1:1 mapping). To use a non-default port, change this value AND set the matching UMLAUTADAPTARREX_LEGACYAPI_PORT variable below to the SAME number."
          Type="Port" Display="always" Required="true" Mask="false">5005</Config>

  <Config Name="Indexer Proxy Port" Target="5006" Default="5006" Mode="tcp"
          Description="TCP HTTP-CONNECT proxy for Prowlarr indexers. Configure this as Prowlarr's HTTP proxy so indexer responses get rewritten on the fly. IMPORTANT: the host port and the container port MUST be identical (1:1 mapping). To use a non-default port, change this value AND set the matching UMLAUTADAPTARREX_PROXY_PORT variable below to the SAME number."
          Type="Port" Display="always" Required="true" Mask="false">5006</Config>

  <Config Name="API Port (variable)" Target="UMLAUTADAPTARREX_LEGACYAPI_PORT" Default="5005" Mode=""
          Description="In-container bind port for the Fastify API. MUST match the 'API Port' mapping above (1:1: host port = container port = this value). The image reads ONLY this branded variable -- the legacy PORT fallback was removed in 1.2.2. Leave at 5005 unless you also remapped the 'API Port' above to the same number."
          Type="Variable" Display="advanced" Required="false" Mask="false">5005</Config>

  <Config Name="Web UI Port (variable)" Target="UMLAUTADAPTARREX_WEBUI_PORT" Default="5007" Mode=""
          Description="In-container bind port for the Next.js Web UI. MUST match the 'Web UI Port' mapping above (1:1: host port = container port = this value). The image reads ONLY this branded variable -- the legacy WEB_PORT fallback was removed in 1.2.2. Leave at 5007 unless you also remapped the 'Web UI Port' above to the same number."
          Type="Variable" Display="advanced" Required="false" Mask="false">5007</Config>

  <Config Name="Indexer Proxy Port (variable)" Target="UMLAUTADAPTARREX_PROXY_PORT" Default="5006" Mode=""
          Description="In-container bind port for the Prowlarr HTTP-CONNECT proxy. MUST match the 'Indexer Proxy Port' mapping above (1:1: host port = container port = this value). When set, it overrides the value stored in the database on every start and the Settings UI shows the proxy port read-only. Leave at 5006 unless you also remapped the 'Indexer Proxy Port' above to the same number."
          Type="Variable" Display="advanced" Required="false" Mask="false">5006</Config>

  <Config Name="App Data" Target="/data" Default="/mnt/user/appdata/umlautadaptarrex" Mode="rw"
          Description="Persistent storage for the SQLite database (umlautadaptarrex.db) and the title-cache. Do NOT delete - it contains all your *Arr connections, API keys and matching state. Ownership is fixed automatically on container start (see PUID/PGID)."
          Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/umlautadaptarrex</Config>

  <Config Name="PUID" Target="PUID" Default="99" Mode=""
          Description="UID the app runs as. Default 99 = Unraid `nobody`, which matches the rest of your appdata shares. The entrypoint chowns /data to PUID:PGID on every start."
          Type="Variable" Display="always" Required="false" Mask="false">99</Config>

  <Config Name="PGID" Target="PGID" Default="100" Mode=""
          Description="GID the app runs as. Default 100 = Unraid `users` group."
          Type="Variable" Display="always" Required="false" Mask="false">100</Config>

  <Config Name="TZ" Target="TZ" Default="Europe/Berlin" Mode=""
          Description="IANA timezone (e.g. Europe/Berlin, UTC, America/New_York). Affects timestamps in logs and the Web UI."
          Type="Variable" Display="always" Required="false" Mask="false">Europe/Berlin</Config>

  <Config Name="LOG_LEVEL" Target="LOG_LEVEL" Default="info" Mode=""
          Description="Pino log level: trace, debug, info, warn, error, fatal."
          Type="Variable" Display="advanced" Required="false" Mask="false">info</Config>

  <Screenshot>https://raw.githubusercontent.com/xpsony/UmlautAdaptarrEX-Unraid-Template/main/assets/dashboard.png</Screenshot>
  <Screenshot>https://raw.githubusercontent.com/xpsony/UmlautAdaptarrEX-Unraid-Template/main/assets/plugins.png</Screenshot>
</Container>
